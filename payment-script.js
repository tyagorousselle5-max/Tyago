let currentPaymentMethod='card';document.addEventListener('DOMContentLoaded',function(){displayOrderSummary()});function displayOrderSummary(){const cart=JSON.parse(localStorage.getItem('cart'))||[];const orderItemsDiv=document.getElementById('order-items');orderItemsDiv.innerHTML='';if(cart.length===0){orderItemsDiv.innerHTML='<p>Panier vide</p>';return}let subtotal=0;cart.forEach(item=>{const itemTotal=item.price*item.quantity;subtotal+=itemTotal;const itemDiv=document.createElement('div');itemDiv.className='order-item';itemDiv.innerHTML=`<div class="item-info"><div class="item-name">${item.name}</div><div class="item-quantity">Qty: ${item.quantity}</div></div><div class="item-price">${itemTotal.toFixed(2)}€</div>`;orderItemsDiv.appendChild(itemDiv)});const shipping=5;const tax=(subtotal+shipping)*.2;const total=subtotal+shipping+tax;document.getElementById('subtotal').textContent=subtotal.toFixed(2);document.getElementById('shipping').textContent=shipping.toFixed(2);document.getElementById('tax').textContent=tax.toFixed(2);document.getElementById('final-total').textContent=total.toFixed(2)}function selectPaymentMethod(method){currentPaymentMethod=method;document.getElementById('card-form').classList.remove('active');document.getElementById('paypal-form').classList.remove('active');document.getElementById('stripe-form').classList.remove('active');document.getElementById(method+'-form').classList.add('active')}function processPayment(){if(!document.getElementById('terms-checkbox').checked){showError('Veuillez accepter les conditions');return}if(currentPaymentMethod==='card'){processCardPayment()}else if(currentPaymentMethod==='paypal'){processPayPalPayment()}else if(currentPaymentMethod==='stripe'){processStripePayment()}}function processCardPayment(){const name=document.getElementById('card-name').value.trim();const email=document.getElementById('card-email').value.trim();const number=document.getElementById('card-number').value.trim();const expiry=document.getElementById('card-expiry').value.trim();const cvv=document.getElementById('card-cvv').value.trim();const address=document.getElementById('billing-address').value.trim();if(!name||!email||!number||!expiry||!cvv||!address){showError('Veuillez remplir tous les champs');return}simulatePayment(()=>{completePayment('Carte bancaire',email,name)})}function processPayPalPayment(){const email=document.getElementById('paypal-email').value.trim();if(!email){showError('Email requis');return}simulatePayment(()=>{completePayment('PayPal',email,'PayPal')})}function processStripePayment(){const email=document.getElementById('stripe-email').value.trim();const card=document.getElementById('stripe-card').value.trim();if(!email||!card){showError('Tous les champs requis');return}simulatePayment(()=>{completePayment('Stripe',email,'Stripe')})}function simulatePayment(callback){const btn=document.querySelector('.btn-pay');btn.textContent='⏳ Traitement...';btn.disabled=true;setTimeout(()=>{btn.textContent='Payer maintenant';btn.disabled=false;callback()},2000)}function completePayment(method,email,name){const orderNumber='CMD-'+Date.now();const total=document.getElementById('final-total').textContent;localStorage.removeItem('cart');document.getElementById('order-number').textContent='Commande: '+orderNumber;document.getElementById('success-message').textContent='Paiement de '+total+'€ confirmé. Confirmation envoyée à '+email;document.getElementById('success-modal').style.display='block'}function showError(message){document.getElementById('error-message').textContent=message;document.getElementById('error-modal').style.display='block'}function closeErrorModal(){document.getElementById('error-modal').style.display='none'}function cancelPayment(){if(confirm('Annuler la commande?')){window.location.href='index.html'}}function redirectHome(){window.location.href='index.html'}window.onclick=function(event){const success=document.getElementById('success-modal');const error=document.getElementById('error-modal');if(event.target===success){success.style.display='none';redirectHome()}if(event.target===error){error.style.display='none'}}